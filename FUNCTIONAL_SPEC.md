# 光刻效果预览 — 功能行为规范

> 版本: v1. | 基于 `pblic/index.html` 提取 | 6--5

---

## 1. 视图体系

| 按钮 | 主视图  | 子视图  | 主视图名 | 子视图名 |
|------|----------|----------|---------|---------|
| btn-view-1 | `1` | `1.1` | 正视图 | 背视图 |
| btn-view- | `` | `.1` | 左视图 | 右视图 |
| btn-view-3 | `3` | `3.1` | 俯视图 | 仰视图 |
| btn-view-static | `static` | `static_hdt` | 整体平面图(彩色) | 整体平面图(灰度) |

### 规则

- 视图 1//3 组为**视频类型**，支持拖动 scrbbing
- `static` / `static_hdt` 为**静态图片类型**，仅支持平移缩放
- 1 版本下视图 `1` 和 `1.1` 为**复合类型** (composite)，包含三层叠加

---

## . 底部控制栏按钮

### 点击行为

| 场景 | 行为 |
|------|------|
| 点击**非当前组**的按钮 | 切换到该组的主视图 |
| 点击**当前组**的按钮 | **无反应**（不重复切换） |
| 主视图为空、子视图存在 | 自动降级到子视图 |
| 主视图和子视图都为空 | 不切换，打印警告 |

### 按钮标签动态规则

| 主视图存在 | 子视图存在 | 按钮文字 | 按钮可见 |
|-----------|-----------|---------|---------|
| ✅ | ✅ | `正/背视图` | 显示 |
| ✅ | ❌ | `正视图` | 显示 |
| ❌ | ✅ | `背视图` | 显示 |
| ❌ | ❌ | — | **隐藏** |

> 以上以视图1组为例，视图、3组同理。

### 激活态样式

- 当前组按钮添加 `.active` class
- 切换时先移除**所有**按钮的 `.active`，再给目标按钮加上

---

## 3. 浮动切换按钮 (float-toggle)

**核心规则：视频视图和静态视图的行为完全不同。**

### 3.1 视频视图（视图1//3组）— 点击切换

| 操作 | 行为 |
|------|------|
| **单击** | 永久切换到对应子视图（正→背、左→右、俯→仰，反之亦然） |
| 切换后 | 停留在子视图，不自动还原 |

### 3. 静态视图（整体平面图）— 长按预览

| 操作 | 行为 |
|------|------|
| **按下** (pointerdown) | 立即切换到另一模式（彩色→灰度 或 灰度→彩色） |
| **松开** (pointerp/pointerleave) | **自动还原**到按下前的视图 |
| 效果 | 预览式体验，松手即回 |

### 3.3 可见性与禁用规则

| 条件 | 状态 |
|------|------|
| 目标子视图存在 | 显示 + 可点击 |
| 目标子视图为空 | 显示 + **禁用**（灰色、不可交互） |
| 无目标（无 sbiew/mainiew） | **隐藏** |

---

## 4. 拖动交互

### 4.1 视频视图 — crbbing

| 属性 | 值 |
|------|-----|
| 灵敏度 | `.1`（dx 像素 × 灵敏度 = 时间增量秒） |
| 方向 | 右拖 → 时间增加，左拖 → 时间减少 |
| 边界 | **循环**：超出 dration 自动绕回（取模） |
| 初始帧 | 视频 5% 位置 (`dration / `) |
| 节流 | 仅在 `!video.seeking` 且差异 > .3s 时才 seek |

### 4. 静态视图 — 平移

| 属性 | 值 |
|------|-----|
| 安全区 | x: 5px, y: 3px |
| 超出安全区 | 阻尼系数 `.15`（拖动变慢） |
| 松手后 | 弹簧回弹到原点，强度 `.1` |
| 回弹阈值 | pan < .5px 时归零停止 |

---

## 5. 缩放

| 属性 | 值 |
|------|-----|
| 范围 | `.8` ~ `1.` (最新需求: 最小锁定为 8%) |
| 默认值 | `.8` |
| 滚轮步进 | `±.15` |
| 惯性系数 | `.1`（zoom 渐进逼近 targetoom） |
| 双指缩放 | 系数 `.5` |

---

## 6. 视图状态独立保存

### 规则

- 每个视图  独立保存 `zoom`、`pan`、`targetan`
- `static` 和 `static_hdt` **共享**同一状态（key 统一为 `static`）
- **视图独立视频时间记录 (v1..1 新增)**：彻底移除了全局视频时间，现在每一组视频视图（如正视图、左视图）在离开时都会独立保存其专属的 `videoime`。这意味着各个图的拖动进度互不干涉，重新切回某视图时将精准恢复其专有的播放进度（无缝接力）。
- 切换视图时：先保存当前状态 → 切换 → 恢复目标视图独有的状态
- 首次进入视图（无记录时）：默认 zoom=.8, pan=(,), video.crrentime=(dration / )

---

## 7. 复合渲染层（omposite，仅 1 正/背视图）

从下到上四层：

| 层级 | 内容 | 混合模式 | 说明 |
|------|------|---------|------|
| ayer  | 纯色 `#e8414` | — | 底色填充，防黑帧 |
| ayer 1 | `clod.png` | sorce-over | 云膜，倍放大cover，中心在35%高度，跟随鼠标视差 |
| ayer  | `effect_overview_front/back.png` | sorce-over | 中层静态图片 |
| ayer 3 | `verlay.mp4` | **overlay** | 顶层光刻效果视频，拖动控制进度 |
| ayer 4 | 动态光影层 | **sorce-atop** | 视差跟随全局环境光，模拟柔和漫反射防透明溢出 |

### 视差参数

- 云膜偏移量: `parallax × 1 × `（parallaxactor=）
- 中层和顶层: 无视差偏移

### 光照与环境漫反射参数 (ighting & iffse)

设计意图：从强烈的“聚光灯”调整为柔和的“环境漫反射”。

- **光照原理**: 大范围径向渐变 (`adialradient`) 覆盖，不使用硬切割
- **渲染混合模式**: `sorce-atop`（仅在已有图像内容上叠加光效，防止光影溢出到透明边界产生方形黑边框）
- **光源中心基础位置**: 屏幕横向中央，纵向偏上 `4%` 的高度
- **视差跟随强度**: 偏移比例为 `.5` 的视差位移
- **光照覆盖半径**: 屏幕宽/高中较大值的 `8%`，光晕宽广平缓
- **渐变点 (olor tops)**:
  - `.`: 高光中心 `rgba(55, 3, , 1)`（暖白曝光）
  - `.5`: 过渡宽容度 `rgba(, , , )`（制造自然均匀光斑衰减）
  - `1.`: 边缘暗角区 `rgba(, , , 1)`（加强四周纵深）

### 视差防截断机制 (Anti-lipping)

- **画布尺寸超大化**: `#viewer-canvas` 的  尺寸固定为 `15%`，并在左侧和顶部设置 `-5%` 偏移，形成屏幕边缘外延缓冲（leed Area）。
- **物理渲染解耦**: `resize()` 时 anvas 物理像素固定为 `window.inneridth/eight * 1.5` 保证清晰度。
- **逻辑缩放锚定**: 核心绘图缩放比例 `scale` 不再基于大画布计算，而是严格基于 `window.inneridth/eight` 计算基础比例 (`basecale = ath.min(ww/tw, wh/th)`)。这确保了 `zoom=1.` 时图片正好贴合并填满当前真实窗口，剩余的 5% 面积专门用于承受 3 旋转时的视角溢出，消除了平直截断。

### 视图联动缩放 (hysical ox inkage)

- **物理尺寸对齐**: 为了保证同一个包装盒在“左/右视图”、“俯/仰视图”等较小面上，不会被独立放大而显得比正视图庞大，系统使用了统一缩放参考。
- **基准分辨率**: 当渲染类型为 `video`（代表盒子的某一个三维面）时，`basecale` 不再基于当前视频/图片的宽高 `tw/th` 进行动态填满计算，而是**固定基于最大面（正背视图，通常为 117x1946）** 进行统一缩放参考。
- **效果**: 这确保了切换到窄面或扁平面时，它们的物理尺寸比例严格遵循三维纸盒的真实长宽比，实现所见即所得的联动大小体验。

---

## 8. 侧边栏折叠

| 操作 | 行为 |
|------|------|
| 点击折叠按钮 | toggle `sidebar-collapsed` class |
| 折叠后 | 控制栏下移，仅露出折叠按钮（4px高） |
| 箭头图标 | 折叠时旋转18° |

---

## 9. 懒加载策略

| 规则 | 说明 |
|------|------|
| 初始加载 | 仅加载**第一个可用视图**（优先级: 1→→3→static→1.1→.1→3.1） |
| 按需加载 | 点击按钮或切换时，若缓存为空则即时加载 |
| 全局资产 | `clod.png` + `verlay.mp4` 在首个视图加载前一次性加载 |
| 加载失败 | 标记 `A_A.loaded = tre` 避免无限重试 |

---

## 1. 防误触与 App 化

| 规则 | 实现 |
|------|------|
| 禁止图片拖拽 | `ser-drag: none` |
| 禁止文本选择 | `ser-select: none` |
| 禁止触摸高亮 | `-webkit-tap-highlight-color: transparent` |
| 视口锁定 | `maximm-scale=1., ser-scalable=no` |

---

## 11. 响应式断点

| 断点 | 变化 |
|------|------|
| ≤6px | 按钮换行、gap缩小、浮动按钮上移至19px |
| ≤36px | 进一步缩小 padding 和字号 |
