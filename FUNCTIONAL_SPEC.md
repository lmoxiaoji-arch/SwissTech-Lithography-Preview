# 光刻效果预览 — 功能行为规范

> 版本: v1.0 | 基于 `public/index.html` 提取 | 2026-02-25

---

## 1. 视图体系

| 按钮 | 主视图 ID | 子视图 ID | 主视图名 | 子视图名 |
|------|----------|----------|---------|---------|
| btn-view-1 | `1` | `1.1` | 正视图 | 背视图 |
| btn-view-2 | `2` | `2.1` | 左视图 | 右视图 |
| btn-view-3 | `3` | `3.1` | 俯视图 | 仰视图 |
| btn-view-static | `static` | `static_hdt` | 整体平面图(彩色) | 整体平面图(灰度) |

### 规则

- 视图 1/2/3 组为**视频类型**，支持拖动 scrubbing
- `static` / `static_hdt` 为**静态图片类型**，仅支持平移缩放
- V1 版本下视图 `1` 和 `1.1` 为**复合类型** (composite)，包含三层叠加

---

## 2. 底部控制栏按钮

### 点击行为

| 场景 | 行为 |
|------|------|
| 点击**非当前组**的按钮 | 切换到该组的主视图 |
| 点击**当前组**的按钮 | **无反应**（不重复切换） |
| 主视图为空、子视图存在 | 自动降级到子视图 |
| 主视图和子视图都为空 | 不切换，打印警告 |

### 按钮标签动态规则

| 主视图存在 | 子视图存在 | 按钮文字 | 按钮可见 |
|-----------|-----------|---------|---------|
| ✅ | ✅ | `正/背视图` | 显示 |
| ✅ | ❌ | `正视图` | 显示 |
| ❌ | ✅ | `背视图` | 显示 |
| ❌ | ❌ | — | **隐藏** |

> 以上以视图1组为例，视图2、3组同理。

### 激活态样式

- 当前组按钮添加 `.active` class
- 切换时先移除**所有**按钮的 `.active`，再给目标按钮加上

---

## 3. 浮动切换按钮 (float-toggle)

**核心规则：视频视图和静态视图的行为完全不同。**

### 3.1 视频视图（视图1/2/3组）— 点击切换

| 操作 | 行为 |
|------|------|
| **单击** | 永久切换到对应子视图（正→背、左→右、俯→仰，反之亦然） |
| 切换后 | 停留在子视图，不自动还原 |

### 3.2 静态视图（整体平面图）— 长按预览

| 操作 | 行为 |
|------|------|
| **按下** (pointerdown) | 立即切换到另一模式（彩色→灰度 或 灰度→彩色） |
| **松开** (pointerup/pointerleave) | **自动还原**到按下前的视图 |
| 效果 | 预览式体验，松手即回 |

### 3.3 可见性与禁用规则

| 条件 | 状态 |
|------|------|
| 目标子视图存在 | 显示 + 可点击 |
| 目标子视图为空 | 显示 + **禁用**（灰色、不可交互） |
| 无目标（无 subView/mainView） | **隐藏** |

---

## 4. 拖动交互

### 4.1 视频视图 — Scrubbing

| 属性 | 值 |
|------|-----|
| 灵敏度 | `0.010`（dx 像素 × 灵敏度 = 时间增量秒） |
| 方向 | 右拖 → 时间增加，左拖 → 时间减少 |
| 边界 | **循环**：超出 duration 自动绕回（取模） |
| 初始帧 | 视频 50% 位置 (`duration / 2`) |
| 节流 | 仅在 `!video.seeking` 且差异 > 0.03s 时才 seek |

### 4.2 静态视图 — 平移

| 属性 | 值 |
|------|-----|
| 安全区 | x: 500px, y: 300px |
| 超出安全区 | 阻尼系数 `0.15`（拖动变慢） |
| 松手后 | 弹簧回弹到原点，强度 `0.12` |
| 回弹阈值 | pan < 0.5px 时归零停止 |

---

## 5. 缩放

| 属性 | 值 |
|------|-----|
| 范围 | `0.8` ~ `1.2` (最新需求: 最小锁定为 80%) |
| 默认值 | `0.8` |
| 滚轮步进 | `±0.15` |
| 惯性系数 | `0.1`（zoom 渐进逼近 targetZoom） |
| 双指缩放 | 系数 `0.005` |

---

## 6. 视图状态独立保存

### 规则

- 每个视图 ID 独立保存 `zoom`、`pan`、`targetPan`
- `static` 和 `static_hdt` **共享**同一状态（key 统一为 `static`）
- **全局视频时间透传**：记录全局视频时间（`globalVideoTime`），视频视图间切换时**自动继承进度**，消除跳跃感。
- 切换视图时：先保存当前状态 → 切换 → 恢复目标视图状态
- 首次进入视图：默认 zoom=0.8, pan=(0,0), video.currentTime=(duration / 2)

---

## 7. 复合渲染层（Composite，仅 V1 正/背视图）

从下到上四层：

| 层级 | 内容 | 混合模式 | 说明 |
|------|------|---------|------|
| Layer 0 | 纯色 `#e84124` | — | 底色填充，防黑帧 |
| Layer 1 | `cloud.png` | source-over | 云膜，2倍放大cover，中心在35%高度，跟随鼠标视差 |
| Layer 2 | `effect_overview_front/back.png` | source-over | 中层静态图片 |
| Layer 3 | `Overlay.mp4` | **overlay** | 顶层光刻效果视频，拖动控制进度 |

### 视差参数

- 云膜偏移量: `parallax × 12 × 2`（parallaxFactor=2）
- 中层和顶层: 无视差偏移

### 视差防截断机制 (Anti-Clipping)

- **画布尺寸超大化**: `#viewer-canvas` 的 CSS 尺寸固定为 `150%`，并在左侧和顶部设置 `-25%` 偏移，形成屏幕边缘外延缓冲（Bleed Area）。
- **物理渲染解耦**: `resize()` 时 Canvas 物理像素固定为 `window.innerWidth/Height * 1.5` 保证清晰度。
- **逻辑缩放锚定**: 核心绘图缩放比例 `scale` 不再基于大画布计算，而是严格基于 `window.innerWidth/Height` 计算基础比例 (`baseScale = Math.min(ww/tw, wh/th)`)。这确保了 `zoom=1.0` 时图片正好贴合并填满当前真实窗口，剩余的 50% 面积专门用于承受 3D 旋转时的视角溢出，消除了平直截断。

---

## 8. 侧边栏折叠

| 操作 | 行为 |
|------|------|
| 点击折叠按钮 | toggle `sidebar-collapsed` class |
| 折叠后 | 控制栏下移，仅露出折叠按钮（24px高） |
| 箭头图标 | 折叠时旋转180° |

---

## 9. 懒加载策略

| 规则 | 说明 |
|------|------|
| 初始加载 | 仅加载**第一个可用视图**（优先级: 1→2→3→static→1.1→2.1→3.1） |
| 按需加载 | 点击按钮或切换时，若缓存为空则即时加载 |
| 全局资产 | `cloud.png` + `Overlay.mp4` 在首个视图加载前一次性加载 |
| 加载失败 | 标记 `GLOBAL_ASSETS.loaded = true` 避免无限重试 |

---

## 10. 防误触与 App 化

| 规则 | 实现 |
|------|------|
| 禁止图片拖拽 | `user-drag: none` |
| 禁止文本选择 | `user-select: none` |
| 禁止触摸高亮 | `-webkit-tap-highlight-color: transparent` |
| 视口锁定 | `maximum-scale=1.0, user-scalable=no` |

---

## 11. 响应式断点

| 断点 | 变化 |
|------|------|
| ≤600px | 按钮换行、gap缩小、浮动按钮上移至190px |
| ≤360px | 进一步缩小 padding 和字号 |
